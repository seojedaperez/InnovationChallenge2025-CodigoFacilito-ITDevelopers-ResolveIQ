# --- Configuración Principal ---
DEBUG=true
ENVIRONMENT=development

# 1. Azure AI Foundry (CRÍTICO)
# 5.  **Obtener la Connection String**:
#     *   ⚠️ **Importante**: No está en la pantalla principal.
#     *   Busca el icono de **Settings** (engranaje ⚙️) en la barra lateral izquierda (generalmente abajo).
#     *   En la pestaña **Project details**, verás el campo **Project connection string**.
#     *   Copia todo el valor (suele empezar por la región, ej: `eastus2.api...`).
#     *   👉 Pégalo en tu `.env` como `AZURE_AI_PROJECT_CONNECTION_STRING`.
AZURE_AI_PROJECT_CONNECTION_STRING=""
AZURE_AI_PROJECT_NAME="service-desk-agents"

# 2. Azure OpenAI
#     *   Añadir la clave y el endpoint de foundry:
#     `AZURE_OPENAI_API_KEY`= tu_clave_real_de_foundry
#     `AZURE_OPENAI_ENDPOINT`= tu_endpoint_real_de_foundry
AZURE_OPENAI_API_KEY=""
AZURE_OPENAI_ENDPOINT=""

# ### Desplegar el Modelo GPT-4o
# 1.  En el mismo portal de AI Foundry, ve al menú izquierdo **Models + endpoints**.
# 2.  Haz clic en **+ Deploy model** > **Deploy base model**.
# 3.  Busca `gpt-4o`. Selecciónalo y dale a **Confirm**.
# 4.  En la configuración de despliegue:
#     *   **Deployment name**: `gpt-4o` (¡Importante! Debe coincidir con tu `.env`).
#     *   **Deployment type**: Standard.
#     *   Haz clic en **Deploy**.
#     *   👉 Asegúrate de que en tu `.env`, `AZURE_OPENAI_GPT4O_DEPLOYMENT` sea `gpt-4o`.
AZURE_OPENAI_GPT4O_DEPLOYMENT="gpt-4o"

# 3. Azure AI Search
# 4.  Una vez creado, ve al recurso:
#     *   **Endpoint**: Está en la vista "Overview". Copia la URL (ej. `https://xxx.search.windows.net`).
#         *   👉 Pégalo en `AZURE_SEARCH_ENDPOINT`.
#     *   **Keys**: Ve al menú izquierdo **Settings** > **Keys**.
#     *   Copia la **Primary admin key**.
#         *   👉 Pégalo en `AZURE_SEARCH_KEY`.
AZURE_SEARCH_ENDPOINT=""
AZURE_SEARCH_KEY=""
AZURE_SEARCH_INDEX_NAME="knowledge-base"

# 4. Azure Cosmos DB
# 5.  Una vez creado, ve al recurso:
#     *   Ve al menú izquierdo **Settings** > **Keys**.
#     *   **URI**: Copia la URI.
#         *   👉 Pégalo en `AZURE_COSMOS_ENDPOINT`.
#     *   **Primary Key**: Copia la clave primaria.
#         *   👉 Pégalo en `AZURE_COSMOS_KEY`.
AZURE_COSMOS_ENDPOINT=""
AZURE_COSMOS_KEY=""
AZURE_COSMOS_DATABASE_NAME="servicedesk"

# 5. Azure Content Safety
# 3.  Una vez creado:
#     *   Ve a **Resource Management** > **Keys and Endpoint**.
#     *   **Endpoint**: Copia la URL.
#         *   👉 Pégalo en `AZURE_CONTENT_SAFETY_ENDPOINT`.
#     *   **Key 1**: Copia la clave.
#         *   👉 Pégalo en `AZURE_CONTENT_SAFETY_KEY`.
AZURE_CONTENT_SAFETY_ENDPOINT=""
AZURE_CONTENT_SAFETY_KEY=""

# 6. Telemetría (Opcional)
# 2.  Una vez creado, en la vista "Overview", copia el **Connection String**.
#     *   👉 Pégalo en `APPLICATIONINSIGHTS_CONNECTION_STRING`.
APPLICATIONINSIGHTS_CONNECTION_STRING=""

# --- Configuración de Aplicación ---
CONFIDENCE_THRESHOLD_AUTO_RESOLVE=0.8
CONFIDENCE_THRESHOLD_ESCALATE=0.5
SPEECH_REGION="eastus"

# 7. Azure AD Authentication
# 1. En el [Portal de Azure](https://portal.azure.com), ve a **Microsoft Entra ID** (Azure AD) > **App registrations**.
# 2. Haz clic en **+ New registration**.
# 3. Configura:
#    * **Name**: `service-desk-agents-app`.
#    * **Supported account types**: Selecciona el que corresponda a tu organización.
#    * **Redirect URI** (Frontend): Tipo `Web`, URL del frontend (ej. `http://localhost:5173`).
# 4. Haz clic en **Register**.
# 5. En la vista del app:
#    * **Application (client) ID**: Copia el GUID.
#      * 👉 Pégalo en `AZURE_AD_CLIENT_ID` (backend) y `VITE_AZURE_AD_CLIENT_ID` (frontend).
#    * **Directory (tenant) ID**: Copia el GUID.
#      * 👉 Pégalo en `AZURE_AD_TENANT_ID` (backend) y `VITE_AZURE_AD_TENANT_ID` (frontend).
# 6. En **Authentication** (menú izquierdo), agrega/valida el **Redirect URI** para SPA/Frontend.
AZURE_AD_CLIENT_ID=""
AZURE_AD_TENANT_ID=""

# 8. Azure Communication Services (Email)
# 3.  **Configurar el Dominio de Email**:
#     *   Ve al recurso de **Email Communication Services** que acabas de crear.
#     *   En el menú izquierdo, ve a **Provision domains**.
#     *   Haz clic en **Add domain** -> **Azure managed domain**.
#     *   Esto creará un dominio gratuito tipo `DoNotReply@xxxx-xxxx.azurecomm.net`.
#     *   Copia la dirección de correo completa (ej. `DoNotReply@81234567-xxxx-xxxx-xxxx-xxxxxxxxxxxx.azurecomm.net`).
#         *   👉 Pégalo en tu `.env` como `EMAIL_SENDER_ADDRESS`.
EMAIL_SENDER_ADDRESS=""

# 5.  **Obtener la Connection String**:
#     *   En el recurso de **Communication Services**, ve a **Settings** > **Keys**.
#     *   Copia la **Connection string** (Primary).
#         *   👉 Pégalo en tu `.env` como `AZURE_COMMUNICATION_CONNECTION_STRING`.
AZURE_COMMUNICATION_CONNECTION_STRING=""
